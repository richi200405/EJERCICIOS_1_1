using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ejericio7_1 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        int numero = 1;

        while (numero <= 100)
        {
            Debug.Log(numero);
            numero++;
        }
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
